import { BrowserMCP } from '../../modules/browser-mcp';
/** @type {import('next').NextConfig} */
const nextConfig = {
    experimental: {
      appDir: true,
    },
  }
  
  module.exports = nextConfig